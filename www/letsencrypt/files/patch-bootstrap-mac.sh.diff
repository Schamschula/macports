--- bootstrap/mac.sh.orig	2015-12-02 17:55:43.000000000 -0600
+++ bootstrap/mac.sh	2015-12-03 19:14:15.000000000 -0600
@@ -1,18 +1,18 @@
 #!/bin/sh -e
-if ! hash brew 2>/dev/null; then
-    echo "Homebrew Not Installed\nDownloading..."
-    ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
-fi
-
-brew install augeas
-brew install dialog
-
-if ! hash pip 2>/dev/null; then
-    echo "pip Not Installed\nInstalling python from Homebrew..."
-    brew install python
-fi
-
-if ! hash virtualenv 2>/dev/null; then
-    echo "virtualenv Not Installed\nInstalling with pip"
-    pip install virtualenv
-fi
+# if ! hash brew 2>/dev/null; then
+#     echo "Homebrew Not Installed\nDownloading..."
+#     ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
+# fi
+# 
+# brew install augeas
+# brew install dialog
+# 
+# if ! hash pip 2>/dev/null; then
+#     echo "pip Not Installed\nInstalling python from Homebrew..."
+#     brew install python
+# fi
+# 
+# if ! hash virtualenv 2>/dev/null; then
+#     echo "virtualenv Not Installed\nInstalling with pip"
+#     pip install virtualenv
+# fi
