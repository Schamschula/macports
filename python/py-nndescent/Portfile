# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           python 1.0

name                py-nndescent
python.rootname     pynndescent
version             0.5.7
revision            0
categories          python
license             BSD
maintainers         {mps @Schamschula} openmaintainer
description         PyNNDescent is a Python nearest neighbor descent for approximate \
                    nearest neighbors.
long_description    {*}${description} It provides a python implementation of Nearest \
                    Neighbor Descent for k-neighbor-graph construction and approximate \
                    nearest neighbor search.
supported_archs     noarch
homepage            https://github.com/lmcinnes/pynndescent

# Note the py310 subport cannot be built as py310-numba is unsupported
python.versions     37 38 39

checksums           rmd160  76dd02384a88667ae2465c2a9f807ab06291b871 \
                    sha256  ecb395255fa36a748b5870b4ba0300ea0f7da8b1964864b8edd62577a84dfd7d \
                    size    1136439

if {${name} ne ${subport}} {
    depends_build-append \
                    port:py${python.version}-setuptools

    depends_lib-append  \
                    port:py${python.version}-importlib-metadata \
                    port:py${python.version}-joblib \
                    port:py${python.version}-llvmlite \
                    port:py${python.version}-numba \
                    port:py${python.version}-scikit-learn \
                    port:py${python.version}-scipy

    livecheck.type  none
}
