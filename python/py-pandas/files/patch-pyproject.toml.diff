--- pyproject.toml.orig	2025-08-21 03:42:59
+++ pyproject.toml	2025-08-25 18:09:46
@@ -2,13 +2,13 @@
 # Minimum requirements for the build system to execute.
 # See https://github.com/scipy/scipy/pull/12940 for the AIX issue.
 requires = [
-    "meson-python>=0.13.1",
-    "meson>=1.2.1,<2",
+    "meson-python",
+    "meson",
     "wheel",
-    "Cython<4.0.0a0",  # Note: sync with setup.py, environment.yml and asv.conf.json
+    "Cython",  # Note: sync with setup.py, environment.yml and asv.conf.json
     # Force numpy higher than 2.0rc1, so that built wheels are compatible
     # with both numpy 1 and 2
-    "numpy>=2.0",
+    "numpy",
     "versioneer[toml]"
 ]
 
