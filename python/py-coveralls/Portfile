# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0
PortGroup           python 1.0

set real_name       coveralls
name                py-${real_name}
github.setup        coveralls-clients coveralls-python 1.8.1
revision            0

license             MIT
maintainers         {mps @Schamschula} openmaintainer
description         coveralls.io is a service for publishing your coverage stats online.
long_description    ${description} This package provides seamless integration with \
                    coverage.py (and thus py.test, nosetests, etc...) in your Python \
                    project.
platforms           darwin
worksrcdir          coveralls-python-${version}

python.versions     27 37

checksums           rmd160  33ac65167614a392f3c82b1bab0493ece6afc797 \
                    sha256  d47e3a7f779505af76848492f8f9d981af888a8f4f467ec9e9279213f86b6cf1 \
                    size    23277

if {${name} ne ${subport}} {
    depends_lib-append  \
                    port:py${python.version}-pytest-runner \
                    port:py${python.version}-setuptools
}
